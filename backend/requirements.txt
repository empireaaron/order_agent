# FastAPI & ASGI Server
fastapi
uvicorn[standard]
python-multipart
python-dotenv

# Database
sqlalchemy
pymysql
alembic

# Authentication & Security
python-jose[cryptography]
passlib[bcrypt]
bcrypt
python-dotenv

# Pydantic & Validation
pydantic
pydantic-settings
email-validator

# AI / LLM / LangGraph
langchain
langchain-openai
langchain-community
langgraph
langchain-text-splitters
docx2txt
unstructured

# AI/LLM SDK (选择一个主要的，这里用 OpenAI)
openai
dashscope

# Vector Database
pymilvus

# Embeddings (使用 HuggingFace 本地模型，无需 API)
sentence-transformers
# File Storage
minio

# Document Processing
python-docx
pypdf
pdfplumber

# HTTP Client (LangChain 需要)
httpx

# Utils
tenacity

# Optional: distributed short-term memory (Redis backend)
# Cache & Storage (Optional)
redis  # 用于分布式对话记忆存储，可选依赖